#include "tensor.fxx"

namespace looney::math {
  template <class T, class CONT>
  class tensor {
    public:
      typedef std::int64_t integer_t;
      typedef std::vector<integer_t> vrank_t;
    private:

  };
};